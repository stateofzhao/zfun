<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.diagramsf">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity android:name=".simple.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity android:name=".simple.letternavigation.LetterNavigationActivity"/>
    <activity android:name=".simple.usecase.InteractorTestActivity"/>
    <activity android:name=".simple.binary.BinaryActivity">
    </activity>
    <activity android:name=".simple.pullrefresh.PullRefreshActivity">
    </activity>
  </application>

</manifest>